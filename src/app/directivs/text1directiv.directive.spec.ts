import { Text1directivDirective } from './text1directiv.directive';

describe('Text1directivDirective', () => {
  it('should create an instance', () => {
    const directive = new Text1directivDirective();
    expect(directive).toBeTruthy();
  });
});
